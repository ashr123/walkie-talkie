// Umbrella (aggregator) build. There is intentionally nothing to configure here:
// shared Java settings live in the `walkietalkie.java-conventions` precompiled script
// plugin (see buildSrc/), which each module applies in its own `plugins {}` block.
//
// Build everything with:  JAVA_OPTS= ./gradlew build
