# Integration tests (@SpringBootTest, RANDOM_PORT) run on plain HTTP — transport TLS is exercised by the
# boot smoke test / manual runs, not the suite. Without this, the default-on TlsConfiguration would boot
# every integration test over HTTPS:8443 and the ws:// / http:// test clients would fail the TLS handshake.
walkie.tls.enabled=false
